{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Debug en Localhost",
            "url": "http://localhost:8080/",
            "webRoot": "${workspaceFolder}",
            "preLaunchTask": "Iniciar Servidor Local"
        },
        {
            "type": "chrome",
            "request": "launch",
            "name": "Abrir index.html directamente",
            "url": "file://${workspaceFolder}/index.html",
            "webRoot": "${workspaceFolder}"
        }
    ]
}
